pre-commit:
  jobs:
    - name: Build README
      files: fd . 'Hannes Knoll'
      run: nu main.nu
      stage_fixed: true

    - name: Check links
      glob: "*.md"
      run: lychee -t 1 {staged_files}
      stage_fixed: true

    - name: Find leaks
      run: gitleaks git -v


pre-commit:
  jobs:
    - name: Build README
      run: |
        nu main.nu
        git add README.md

    - name: Check links
      run: lychee -t 1 README.md

    - name: Find leaks
      run: gitleaks git -v

